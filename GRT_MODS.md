# GRT_MODS — every in-place edit to upstream-owned files

This fork keeps its own code in **`openpilot/grt/`** (plus `third_party/mapd/`). Upstream files
carry only thin hooks. This table is the **sync checklist**: after rebasing onto a new upstream
openpilot release, re-verify each row.

Fast audit: `grep -rn "GRT-MOD" openpilot/ --include=*.py --include=*.h --include=*.inc --include=*.capnp`

Categories: **A** new fork-owned file (no conflict) · **B** registration splice (low risk) ·
**C** behavioural injection (HIGH risk — re-verify semantics, not just that it applies) ·
**D** schema (low risk by design, but wire-critical).

## In-place edits to upstream files

| File | Where | Cat | What / why |
|---|---|---|---|
| `openpilot/cereal/custom.capnp` | reserved slots 17/18/19 | D | Renamed to `MapdExtendedOut`/`MapdIn`/`MapdOut` **keeping struct IDs**; added mapd support structs + Mapd-prefixed enums. Wire-critical: IDs and field ordinals must never change. |
| `openpilot/cereal/log.capnp` | union members `@143/@144/@145` | D | Renamed to `mapdExtendedOut`/`mapdIn`/`mapdOut`. Ordinals unchanged; union **discriminants stay 141/142/143** — that is what the binary puts on the wire. |
| `openpilot/cereal/custom.capnp` | reserved slot 16 | D | Renamed to `GrtSetSpeedState` **keeping the struct ID**, with 5 fields added. Fork-owned card→selfdrived channel; nothing outside this repo consumes it. Speeds are **km/h**, unlike the mapd structs. |
| `openpilot/cereal/log.capnp` | union member `@142` | D | `customReserved16` → `grtSetSpeedState`. Ordinal unchanged, so the wire discriminant stays **140** and mapd's 141/142/143 do not move. Asserted by `grt/tests/test_schema_conformance.py`. |
| `openpilot/cereal/services.py` | end of `_services` dict | B | 3 inlined mapd service entries + `grtSetSpeedState` (20 Hz, SMALL, `should_log=False`). **Deliberately inlined, not imported** — services.py runs as a standalone script at build time (no repo root on `sys.path`), so an `openpilot.*` import here breaks the build. Queue size must stay `MEDIUM`. |
| `openpilot/common/params_keys.h` | last line of `keys` initializer | B | One `#include "openpilot/common/grt_params_keys.inc"` inside the braces, so new fork params never touch this file again. |
| `openpilot/system/manager/process_config.py` | after `procs` list | B | `procs += grt_procs()`. |
| `openpilot/selfdrive/controls/plannerd.py` | import block + `SubMaster(...)` | B | `+ GRT_SUB` on the service list (now `mapdOut` **and** `grtSetSpeedState`, the latter so `scc_map` obeys only limits the driver AUTHORISED), **plus `ignore_alive`/`ignore_valid`/`ignore_avg_freq` for both**. The ignores are NOT optional: mapd only runs when OSM tiles are installed, and without them a missing `mapdOut` makes `sm.all_checks()` False, marking `longitudinalPlan` INVALID and faulting longitudinal control on any device without tiles (verified on the car). Upstream occasionally adds entries to this list — expect a trivial re-resolve. |
| `openpilot/selfdrive/selfdrived/selfdrived.py` | import block; `ignore` list + `SubMaster(...)`; `update_alerts` | **C** | `+ GRT_SUB_SELFDRIVED` on both the service list **and the `ignore` list**, plus one line `self.AM.add_many(frame, grt_hooks.set_speed_alerts(...))` between the existing `add_many` and `process_alerts`. **The ignore entry is the most safety-critical in this table**: `sm.all_checks()` is called UNSCOPED at ~`:381` (raises `commIssue`) and ~`:469`, where it gates `self.initialized` — so a fork service with no publisher would BLOCK ENGAGEMENT ENTIRELY, not merely invalidate one message. The alert is a plain `Alert` with a fork-owned `alert_type` string; `AlertManager.add_many` keys on that, so no `EventName` enumerant and no schema change is needed. |
| `openpilot/selfdrive/selfdrived/selfdrived.py` | `not_running` set (~line 341) | **C** | Subtracts `GRT_IGNORED_PROCESSES` so mapd cannot raise `processNotRunning` and block engagement. Adapted, **not** copied from sunnypilot — this openpilot version has no `self.ignored_processes`. If upstream restructures this block, re-apply by meaning, not by patch. |
| `openpilot/selfdrive/car/card.py` | import block; `SubMaster(...)`; `PubMaster(...)`; after `initialize_v_cruise`; `state_publish` | **C** | `+ GRT_SUB_CARD` on the service list **with the same three ignore lists as plannerd**, `+ GRT_PUB_CARD` on the PubMaster, a `grt_engage_edge` local mirroring upstream's own engage condition, one hook line `grt_hooks.track_set_speed(...)`, and a 20 Hz `grtSetSpeedState` publish in `state_publish`. The hook MUST stay after `initialize_v_cruise` (which resets `v_cruise` on the engage edge) and before the `CS.vCruise` assignment. **Deliberately here and not in `cruise.py`**, contrary to the original plan: `update_v_cruise(CS, enabled, is_metric)` is called directly by `selfdrive/car/tests/test_cruise_speed.py` in five places, so changing its signature to pass a `SubMaster` would break an upstream test suite. card's own health checks are scoped (`all_checks(['carControl'])`), so `mapdOut` cannot invalidate `carOutput`. |
| `openpilot/selfdrive/car/card.py` | `SubMaster(...)` (plain list, not `GRT_SUB_CARD`); before `self.CI.apply(...)` | **C** | Lead-vehicle dash icon, Tier 2. `+ 'radarState'` on the base service list — **not** the ignore-listed `GRT_SUB_CARD`, because it's a stock always-published service, needing none of `mapdOut`'s ignore-list treatment. One hook: stashes `(dRel, vRel, present)` from `sm['radarState'].leadOne` onto `self.CI.CS._grt_lead` immediately before `self.CI.apply(CC, now_nanos)`, which forwards `self.CS` unchanged into `CarController.update()` — no `interfaces.py`/`apply()` signature change, no other car brand touched. |
| `openpilot/selfdrive/ui/mici/onroad/model_renderer.py` | one GRT-MOD block in `_update_leads` | **A** | **hook 11c, DISPLAY ONLY.** Filters the lead distance the HUD draws through `grt/lead_filter.py` (Hampel-7 outlier identifier -> RangeRate(.20,.008)). This car is vision-only -- no radar -- so stock radard publishes the model's raw monocular depth with NO filtering, and 27.8% of far-lead frames move >3 m in one 20 Hz frame, which is physically impossible; that jitter is drawn on screen today. Measured vs stock's filter over 5.63 h: 0.89 m RMS vs 1.36, and on the 09:57 FCW emergency it reads a closing lead 6.30 m too far at worst vs stock's 6.75 m (i.e. safer in the unsafe direction). Runs in the UI process, which is outside the safety chain: it reaches neither the planner nor the car, and falls back to the raw value on any exception, so a bug degrades to exactly today's behaviour. Called for EVERY lead every frame including absent ones -- two wiring bugs were caught in review by not doing that: leadOne/leadTwo sharing one filter instance, and never seeing `present=False` so state was smoothed across a dropout. `lead_filter.py` deliberately has no module-level openpilot imports so the UI does not inherit plannerd's dependency graph. See captains_log.md 2026-09-02/03. |
| `openpilot/selfdrive/controls/lib/longitudinal_planner.py` | import block; 8 GRT-MOD blocks around `get_cruise_accel` and `min(candidates)` | **C** | **EIGHT hooks, 0 deletions. The highest-risk row in this table.** In execution order: **hook 1** `limit_v_cruise()` before `get_cruise_accel` — only ever lowers `v_cruise`, so `forceDecel` still wins; also runs the mapd controller for the frame, which the others depend on. **hook 5** `soften_cruise_decel()` immediately after `get_cruise_accel` — raises the cruise braking floor to `-COAST_DECEL` for plain overspeed; reads the `_v_cruise_lowered` flag that hook 1 set THIS frame, so it must stay after it. **hook 6** (`E2EAccelFloor`), **hook 8** (`HoldSpeed`) and **hook 9** (`AggressiveCandidateRamp`) — all three enter through the single `floor_e2e_accel()` call on `output_a_target_e2e`, so the upstream file still sees only one line. Must stay AFTER hook 1, because a lowered `v_cruise` shrinks the headroom that gates them. **hook 2** `extra_accel_candidates()` appends a hazard candidate — after hook 1, before the `min()`. **hook 10 layer B** `deadband_cruise_accel()` immediately after hook 5 and BEFORE the `min()` — at or below the set speed it returns `ACCEL_MAX`, so the cruise branch cannot veto a raised e2e candidate. **hook 7** `ramp_relaxed_accel()` (`RelaxedAccelRamp`) AFTER the `min()`, so it shapes whichever candidate won rather than biasing the choice. **hook 10 layers A+C** `hold_throttle()` LAST, after hook 7 — sign debounce on the final command, plus the headroom backstop. **hook 11** `far_lead_candidates()` (`grt/far_lead.py`) — appends immediately AFTER hook 2, before the `min()`, RELAXED ONLY. Needs a `stock_min = min(c[0] for c in candidates)` computed inline at the call site (the only hook that needs an argument beyond `sm`/`v_ego` — the MPC/e2e candidates it hands off to are local variables in this same function, not published anywhere it could read them back). See `FAR_LEAD_PREBRAKE_PROMPT.md` and the `far_lead.py` module docstring for the full design and four bugs found so far: gating arming on raw `leadOne.vRel` (caught in testing, false-armed on measurement noise), checking the arm distance at persistence-completion instead of at first lock (caught in testing, made a fully-stopped lead — the single most dangerous case — fail to ever arm), a rising-edge absence-gate requirement that blocked arming on a real 2026-08-27 drive regardless of how hot the danger signal got (caught in production; removed in v2, which arms on continuous presence + a sustained hot signal alone, anchoring the arm-distance check — now 80 m, `ARM_MIN_DIST` — at hot-streak-start instead of first-lock), and (caught in production, 2026-08-28) `a_req`'s distance-scaling letting tiny closing rates arm on ordinary highway noise combined with a release condition that required fully non-negative closing — the slow-dynamics filter rarely reached that quickly on real data, so the floor got held ~9-11 s after a transient resolved; fixed by requiring a real closing rate (`HOT_CLOSING_RATE = 2.78` m/s, ~10 km/h) on both arming and release, cutting highway false-arm time 60.5s → 19.3s over a real 54-min drive at a cost of ~0.3-0.6s less armed time on both prior validated incidents. **EXPERIMENTAL, 2026-08-31, CONCLUDED SAME DAY:** `FLOOR` briefly 0.00 (was -0.40), operator request, testing a softer floor as a fix for a fifth finding (hook 11 arming on top of an approach stock already had under control). Found a sixth, worse bug instead: `FLOOR` also sets the "stock caught up" release threshold (`stock_min <= FLOOR`), and at 0.00 that threshold degenerated from "stock is genuinely braking" to "stock isn't accelerating" — trivially true almost constantly, causing the hook to self-release almost immediately after every arm. Confirmed on a real ~119 km/h approach on the test drive: hook armed correctly, tracked its predicted ramp for 4 frames, then went inert for a full second while the real danger kept developing. `FLOOR` reverted to -0.40 same day. Fifth finding still open — the `stock_min` arm-time gate (decoupled from `FLOOR`) remains the intended fix; see `far_lead.py`'s "FLOOR EXPERIMENT" docstring section for the full evidence. Note §7 of the 2026-08-24 spec lists hook 2 after the `min()`; that is a slip in the spec — hook 2 APPENDS a candidate so it must precede it, and it has not been moved. If upstream changes `A_CRUISE_MIN`, `ACCEL_MIN/MAX` or the `min()` arbitration, re-verify the hook-2 AND hook-6 safety arguments — hook 6 is the only hook in the fork that can make the car LESS cautious (see `openpilot/grt/e2e_floor.py`). **2026-08-25 seam edits** (files stay category A, but the CONTRACTS changed): hook 8's zero-cap moved from 0.0 to −0.20 and its 1 km/h handoff now fires only ABOVE the set speed; hook 6 fades the throttle portion of an abandon at 1.5 m/s^3 instead of dropping in one frame. **Known open defect:** hook 5's skip-guard is `target < v_cruise`, which reads False once hook 3 has already moved the driver-facing set speed down to the posted limit, so the approach is softened when it should keep full authority (captains_log 2026-08-20). **DISTANCE-NEUTRAL ARMING THRESHOLD, 2026-09-04:** the arming gate now tests `a_req_filt` against `hot_a_req_for(dRel)` instead of the bare `HOT_A_REQ`. Because arming needs `a_req = v^2/(2*(d-6)) > HOT_A_REQ`, the CLOSING RATE it demanded grew with distance (3.85 m/s at 80 m, 4.56 at 110 m) -- a far-lead feature was hardest to trigger exactly where it is meant to work, and measurement showed 76% of arming latency on a real drive sat on this one condition. Scaling by `ARM_MIN_DIST/dRel`, floored at `HOT_A_REQ_MIN_SCALE = 0.60`, cancels the growth so the required closing rate is flat (~3.85-3.90 m/s) at every distance. SAFETY: the scale is exactly 1.0 at or below `ARM_MIN_DIST`, so the threshold is bit-identical to before under 80 m, and `ARM_MIN_DIST` already requires the anchor above 80 m -- it can only RELAX, never tighten, so it cannot cause a missed arm the previous criterion caught. Applied at the ARMING GATE ONLY; the armed branch's severity `a_req` stays physically exact because it decides how hard to brake, not whether to. Replay over 11 drives / 6.8 h: 12 shared arms fired earlier (median +0.21 s, max +3.05 s), arms beyond 100 m went 1 -> 5, real arms 16 -> 17, false arms unchanged at 3. 12 new tests in test_far_lead.py pin the near-field identity and the clamp. captains_log.md 2026-09-04 (g). **ANCHOR ON FILTERED RANGE, 2026-09-14:** `dRel_at_hot_start` now takes the alpha-beta filter's position state (`self.filt.x`) instead of a single raw `dRel` sample, and `ARM_MIN_DIST` moves 70 -> 73 m as its paired compensation. Measured over a 29-route corpus: ~58% of hot streaks that had already cleared every other gate died on the `ARM_MIN_DIST` test alone, with ~12% of them within +-5 m of the line -- inside dRel's own 4-6 m frame-to-frame noise. Against a non-causal centred reference the raw sample is biased LOW by 2.14 m (hot-start fires preferentially on frames where the sample dipped below trend), `filt.x` biased HIGH by 0.93 m of filter lag, absolute error roughly halving 2.28 -> 1.26 m. The 3 m net relaxation is why 70.0 could not stay: uncompensated it added 39 arms that clear the genuinely-slower-lead bar only 29% of the time against a 45-46% baseline, whereas 73.0 adds 20 arms at 44% -- as good as the arms already taken -- while keeping every CAP event 70.0 found. Corpus: arms 171 -> 190, arms reaching CAP 17 -> 26, all 17 baseline CAP events still matched, 1 non-CAP baseline arm lost. OPEN CONCERN: only 2 of the 9 new CAP arms are on a genuinely slower lead (baseline 8/17), and they fire at 34-54 m rather than the baseline's 86.5 m median -- a false arm at FLOOR is ~1.7 s of 0.04 g, a false arm at CAP is -2.0 m/s^2. Watch for unexplained hard braking. Rollback is two lines. captains_log.md 2026-09-14 (d). **OBJECT-SWITCH GUARDS + ARM_MIN_DIST 73 -> 65, 2026-09-15:** the OPEN CONCERN above, confirmed and fixed. `leadOne` is an anonymous slot; when radard's lead switches to a nearer object dRel steps, and `_RangeRateFilter` differentiated the step into tens of m/s of phantom closing (a 40 m step ~ -24 m/s) that `a_req` then squared. Every one of the 26 CAP arms on the 29-route corpus under the 09-14 code followed such a switch; by channels independent of hook 11, 10 were traffic pulling away, 8 of them admitted by the filtered anchor lagging above 73 m after the step. Two guards in `_RangeRateFilter.update(z, v_ego)`: STEP (median of last 3 vs previous 3 raw samples > `STEP_GATE_M` 12 m for `STEP_FRAMES` 2 frames, first frame coasted) and a PHYSICAL BOUND (over 0.8 s the range may not close faster than ego speed nor open faster than 15 m/s, +8 m margin). On either the filter re-initialises on the new range (`stepped`) and the hook restarts presence, hot streak and anchor. `ARM_MIN_DIST` 73 -> 65 is an OPERATOR DECISION paired with the guards (never without them: 65 m unguarded re-admits all 10 spurious CAP arms). Corpus replay, 31 TSVs: spurious CAP 10 -> 0, real CAP 7 -> 1 (the others now arm softer, -0.5 to -1.0, where stock was already braking on 4 of 7), arms 190 -> 204, real closes caught 49 -> 56 of 87, added arms mostly FLOOR at 65-73 m with 17 of 25 on a genuinely slower lead (8 of 25 not closing, against 43% for the 09-14 code's own arms). Cost on ordinary driving: the guards fire ~135 times per eligible lead-hour and ~37% of STEP resets are noise-like (range back within 4 m a second later); 6 of the 43 lost 09-14 arms follow such a reset; shared non-CAP arms unchanged (112/134 identical timing, 104/134 identical command). The committed file reproduces the validated replay variant arm-for-arm on all 31 routes. test_far_lead.py 47 -> 63 (16 new: step, reveal, ramped switch, single-frame outlier, stopped car at ego speed, noisy 20 m/s close, 100 s noisy steady follow x3, switch while armed, new object re-earns its arm, pairing pin); the new cases fail on the pre-port file. Rollback: `ARM_MIN_DIST = 73.0` alone (guards-only row), or revert the commit. captains_log.md 2026-09-15 (a); FINDINGS.md §20. **HOOK 11c, FRONT-RUN RECORDER, 2026-09-16:** measurement only, in `hooks.py` beside the existing lead-filter shadow, called from `far_lead_candidates` with what hook 11 just returned and the REAL `stock_min`. Replay cannot answer how often hook 11 actually binds or how much earlier braking starts, because the harness feeds `stock_min = 0.0` and its release times are upper bounds only. One JSON line per armed span (never per frame) into the existing rotating `lead_filter.log`: `bind_s` (seconds the hook's candidate was strictly harder than the planner's best), `gain_s` / `gain_m` (time and range from the arm until stock asked for at least as much braking), `caught`, plus dRel at arm and at hand-off. Own try/except and latch, like every other hook; returns nothing and can change no command. **HAND-OFF BAR ADDED 2026-09-16 (b):** the first 8 real spans showed `caught` false on 7 of 8 while the planner reached -0.40 within 0.1 s of release -- because far_lead releases on `stock_min <= FLOOR`, not on stock matching hook 11's own (harder) command, so the original `gain_s` could never exceed the bind time. The record now also carries `floor_s` / `floor_m` / `handoff`, measured to the FLOOR crossing, which is the bar the hook actually hands off on and the number that answers "how much earlier did braking start". FLOOR is imported from far_lead at the call site so it cannot drift from the release rule. **DEPLOYED DESPITE FAILING VALIDATION, 2026-08-31, attempt 5 (operator override):** `a_req` at both the arming gate and the active-command severity switched from `(v_ego**2-v_lead**2)/(2d)` to the physically-correct `v_filt**2/(2d)`, `HOT_A_REQ` lowered 0.30→0.10, `CAP` widened -1.2→-2.0 (`FLOOR` unchanged, -0.40). Pre-deployment validation (`gap_at_release` against the git-blob-pinned true-deployed baseline, the metric that sank the 2026-08-31 severity-only-split attempt earlier the same day) found this variant regresses 6.61-16.65 m on all three genuine founding incidents — worse than that earlier reverted attempt, 6.7x worse on one route — plus misses a real arm the pre-attempt-5 formula caught. Both advisor consultations recommended against deploying. Operator reviewed the numbers and chose to deploy anyway for a real-world test drive; documented as a deliberate, informed field experiment, not a validated fix. Single self-contained commit; `git revert` restores the prior (`2d4473136`) formula/constants. Full numbers: captains_log.md 2026-08-31, "attempt 5." **BAND-SLOPE ARMING GATE, 2026-09-22 — THE ARMING GATE REPLACED:** hook 11 no longer arms on presence + hot streak + `ARM_MIN_DIST`. It arms when the SLOPE of a lower Bollinger band on the model's range series (`mean(100) - 2*stdev(100)` of `dRel_model`, slope fitted over 40 band samples) CROSSES below -5 m/s, with a radar lead present and `dRel > HANDOFF_DIST` (50 m); it releases when that slope crosses back above 0, or range falls under `HANDOFF_DIST`, or stock reaches FLOOR, or the lead is lost. No persistence timers: the 5 s mean and 2 s slope fit ARE the persistence. This needs a CONTINUOUS range series across frames where radar reports no lead (`leadOne.dRel` is 0.0 then), so `far_lead_candidates` now also reads `modelV2.leadsV3[0].x[0]` and passes it in — `longitudinal_planner.py` itself is UNCHANGED by this edit, the new read is inside `grt/hooks.py`. **The old `eff_vRel_range >= -HOT_CLOSING_RATE` release is DELETED and cannot come back:** this gate fires ~2.5 s before the range-rate filter converges, so that test would fire on the frame after arming and collapse every span to 1-2 frames. **Residual risk:** a slow-but-real close no longer releases on rate, so it holds FLOOR (0.04 g) until closing stops or range < 50 m — the FOURTH BUG's shape, and the main thing the field test is watching. Replay, c7+c8, 0.62 h: 14 arms (22.6/h) vs the old gate's 16 (25.8/h) on the same data, span median 3.10 s / max 11.20 s, 5 of 14 hardened past FLOOR, hardest -0.55; arm times reproduce the offline-validated gate arm-for-arm. Median closure rate over the gate +1.57 → +2.09 m/s. `HOT_A_REQ`, `HOT_PERSIST_S`, `PRESENCE_PERSIST_S`, `ARM_MIN_DIST`, `THRESH_SCALE_DIST` and `hot_a_req_for()` are RETAINED and still tested but no longer called here — hook 11b's `_ArmMirror` reads them to shadow what the old gate would have armed on, which is this field test's comparator. test_far_lead.py 77 → 79, test_hooks.py 60 → 65 (new `test_hook11_wiring` drives the real `far_lead_candidates`, which caught a shipping bug: `fl` at that call site is the INSTANCE, not the module, so reading the offset off it raised AttributeError into the blanket except and silently disabled hook 11). Rollback: `git revert`; `108e5850e` is the pre-change tip. captains_log.md 2026-09-22; FINDINGS.md §21-§22a. **HAND-OFF BAR DECOUPLED FROM FLOOR, 2026-09-22:** `HANDOFF_ACCEL = -0.40` is now its own constant; the release reads it instead of `FLOOR`, and hook 11c measures it too. Deliberately equal to FLOOR, so behaviour-neutral -- replay over c7+c8+cf gives the same 25 arms, durations and commands. FLOOR is an AUTHORITY bar (softest command this hook may issue), HANDOFF_ACCEL a TRUST bar (how much braking from someone else counts as handled); overloading them caused the 2026-08-31 sixth bug. NOTE the replay harness feeds `stock_min = 0.0` so it cannot exercise the hand-off at all -- this rests on unit tests, one reproducing the 2026-08-31 setup exactly. Motivated by FINDINGS.md §25: the hook is soft because it sits clamped at FLOOR for 72% of armed frames, NOT because of filter lag (v_filt over-reads closing) or a starved formula (with the true closing rate a_req is 0.08, softer than FLOOR in 7/7 takeovers). The missing term is the lead's own deceleration, and neither input can carry it -- `leadsV3[0].a[0]` scores a 0.026 gain and sees real braking 11% of the time; differentiating the band slope gives 3.40 m/s^2 RMS and 30% false alarms. FLOOR sweep (0.86 h): arms/armed-time identical at every value, knee at -0.50 (1093 -> 1954 frames more severe than the car actually did, then only +48/+30/+19). **No FLOOR change made** -- the sweep is open loop. captains_log.md 2026-09-22 (d); FINDINGS.md §25-26. **ARMING IS A LEVEL TEST, 2026-09-22:** hook 11 arms when the band slope is at or below `ARM_SLOPE` with a lead present, not only on a crossing. Closes a documented blind spot -- a series already closing faster than ARM_SLOPE when the slope first becomes defined produces no transition, so the gate could never arm on it (route start, and after every gap in the model lead). Two new constants are guards the crossing had been masking: `ARM_CONFIRM_FRAMES = 3` applied only inside `ARM_CONFIRM_DIST = 60.0` (beyond 60 m the gate arms on the FIRST qualifying frame; between HANDOFF_DIST and 60 m the condition must hold 3 consecutive frames). The chatter is near-field -- 20 of 21 degenerate spans armed within 8 m of the bar -- so guarding everywhere charged 0.15 s to every far-field arm to fix a near-field problem. A 55 m boundary was tried and is too low (4 of 5 remaining degenerate spans armed in 55-60 m) and `RE_ARM_HOLD_S = 1.0` (after a stock hand-off the slope is usually still past the bar, so the hook would re-arm next frame and oscillate). RELEASE stays a crossing. Replay c7+c8+cf: 29 arms, median span 3.25 s, armed 107.9 -> 112.8 s, sub-2-frame spans 2 -> 2, re-arms 1 -> 2; every far-field arm fires 0.10 s EARLIER than the margin version -- behaviour-neutral on this corpus; the value is the blind spot, the cost is that the hook no longer arms between 50 and 60 m. captains_log.md 2026-09-22 (e); FINDINGS.md §29. **BAND GATED ON MODEL CONFIDENCE, 2026-09-22:** `_BandSlope` was fed `leadsV3[0].x[0]` on every frame REGARDLESS of `prob`. Below ~0.5 that output is a regression head with no target, so acquisition transients differentiated into phantom closing: on route cf at 13:44:41 prob sat at 0.004-0.011 for 3+ s while x[0] wandered 118-133 m, then fell 131.1 -> 102.8 m in 0.26 s as the model acquired, giving a band slope of -13.26 m/s (2.6x the arm bar) while the gap was actually OPENING. Every other consumer of leadsV3 goes through radard's gate (`radard.py:237-242`: asymmetric filter, instant rise / RC 0.2 decay, publish only when `lead_prob > .5`); the band was the sole unconditioned reader. 11 of 29 arms across c7+c8+cf were below that bar. `update()` now takes `prob` and runs the same filter (`PROB_GATE` 0.5, `PROB_ALPHA` 0.2 -- numerically radard's RC only at DT_MDL=0.05), and `hooks.py` passes `leadsV3[0].prob` as a 10th argument; omitting it reads as always-confident and silently replays the bug, so `harness._guard_step` now demands it. On acquisition the window is RE-SEEDED rather than warmed cold (cold = 7.0 s blind after every lead appears). Seeded at `SEED_SIGMA = 3.0` m, NOT flat: a flat seed starts stdev at zero and, since `band = mean - K*stdev`, stdev growing back drags the band down for 5.0 s after every acquisition, manufacturing the same phantom (-2.8 m/s on synthetics; c8 re-seeds 213 times and gained 4 arms on leads that were not closing). 3.0 m is the measured median 100-sample stdev over 23727 confident windows; worst manufactured slope falls to -1.38 m/s at 4 m noise. Replay c7+c8+cf: 29 arms -> 20, unconfident 11 -> 0, armed 113 -> 95 s, confident arms 18 -> 20. Cost is `SLOPE_N` and irreducible -- with the HANDOFF_DIST refusal the reveal boundary is `d_acq > HANDOFF_DIST + closing * SLOPE_N * DT_MDL` (110 m at 30 m/s); above it the gate arms at +40 frames, below it it declines. Because radard publishes `present` only when ITS filtered prob clears the same bar, `radar present` now implies `band confident` by construction. test_far_lead 99 -> 112, test_hooks 66 -> 68. **EXPOSES A PRE-EXISTING GAP, NOT FIXED HERE:** `_BandSlope` has no physical bound where `_RangeRateFilter` has STEP and PHYS tests -- a ramped 92 -> 48 m model-range switch arms the gate, and so does the git-pinned PRE-change file (frame 173) once the band is warm, as it always is in production; the old test passed only because `feed()` started cold. FINDINGS.md §30, §30a. **PROPORTIONAL STOPPING TARGET, 2026-09-22 (operator request):** the ARMED-branch severity `STOP_MARGIN` goes from a fixed 6.0 m to `STOP_MARGIN_FRAC = 0.5` of `dRel`, so `a_req = eff_vRel_range**2 / (2*max(eff_dRel*(1-FRAC), 1.0))`. Rationale: how hard to brake to bleed off a closing rate scales with the room available, not with a fixed 6 m taken off it; reaching the lead's speed within HALF the present gap is self-similar. With FRAC = 0.5 the denominator is exactly dRel (a_req = v^2/dRel), ~1.9x the old value in the far field, converging near 12 m. SEVERITY ONLY -- the band-slope gate does not read `a_req`, so arming cannot change, and replay confirms 20 arms before and after. `STOP_MARGIN` STAYS 6.0 and must not follow: `hot_a_req_for()` and hook 11b's `_ArmMirror` (hooks.py:824) use it to reproduce the OLD gate, which is this field test's comparator; pinned by test. Replay c7+c8+cf vs a pinned blob: frames at FLOOR 84.8% -> 75.2%, at CAP 0 -> 40, mean -0.444 -> -0.530, hardest -1.510 -> -2.000; 470/1901 frames differ, median 0.329 harder, concentrated at dRel 50-70 m (+0.343, the hand-off zone the operator flagged as too soft). Both CAP spans are on route cf at ~110 km/h behind leads 18.5 and 21.4 kph slower -- genuine, zero false CAP, but the number to watch next drive since a false arm at CAP costs -2.0 m/s^2 against FLOOR's 0.04 g. test_far_lead 112 -> 118. Rollback: `STOP_MARGIN_FRAC = 0.0`, or revert. captains_log.md 2026-09-22 (proportional stopping target). **STEP GUARD REMOVED, 2026-09-24 (operator):** the STEP test in `_RangeRateFilter` (and `STEP_GATE_M`/`STEP_FRAMES`) is deleted pending refinement; the physical bound stays as the only object-switch re-init. Arming unchanged (56 arms either way on c7+c8+cf+d7); severity firmer on camera range jumps -- time at <= -1.0 16.6 -> 23.6 s, at CAP 4.1 -> 5.8 s -- which on d7 all preceded real approaches. Accepted exposure: a jump to a nearer DIFFERENT object inside the physical bound reads as closing, up to CAP. Restore from `7997662e0`. FINDINGS 33. **v_filt ON EVERY FRAME, 2026-09-24 (operator):** `_RangeRateFilter` is created once in `__init__` and updated at the top of `step()` before every early return -- fed `dRel` while a lead is present, the model range otherwise (never the absent 0.0), coasting with neither; the fresh-lock reset is removed. `v_filt` is always a float. Arming unchanged; severity near-neutral (time at <= -1.0 23.6 -> 24.2 s). FINDINGS 34. **PHYSICAL-BOUND SPAN 0.8 -> 0.45 s, 2026-09-24 (operator):** `PHYS_WINDOW` 21 -> 14, `PHYS_SPAN_S` 0.8 -> 0.45 -- the smallest span that still clears the genuine arms on route 000001d7 (0.40 s trips on bookmark 3's camera step). Trips 0.70 -> 1.19/min; replay severity unchanged (24.2 -> 24.3 s at <= -1.0). FINDINGS 35. **BAND SAMPLES EVERY FRAME AGAIN, 2026-09-24 (operator):** `_BandSlope.update` no longer skips low-confidence frames or re-seeds on acquisition (`SEED_SIGMA` removed); arming unchanged. Ends the release blind spot (armed without a slope 28.2 -> 0 s over c7+c8+cf+d7) at the cost of the section-30 acquisition false arms (arms 56 -> 70, 0 -> 20 on low-confidence leads, <= -1.0 24.3 -> 30.4 s). A 2 s confidence condition on arming was measured to keep both gains and was not adopted. FINDINGS 36. **FILTER GAINS 0.10/0.003 -> 0.20/0.0222, 2026-09-24 (operator):** Benedict-Bordner pair; sustained closing read 0.7-1.5 s sooner over c7+c8+cf+d7, time at CAP 6.5 -> 2.8 s, false closing on steady following 2-4x. A 65 m jump spikes v_filt to +-51 m/s for 2 frames (command bounded by JERK_ARM); a lead dropout on exactly such a frame can walk the command to CAP via last_known -- accepted, rare. FINDINGS 37. |
| `opendbc_repo/opendbc/car/hyundai/hyundaicanfd.py` | `create_acc_control()`, `values` dict | C | Adds `SCC_ObjSta` (lead-vehicle dash icon), derived from `enabled`/`hud_control.leadVisible`/`gas_override` — all pre-existing function args, no new plumbing. `ObjValid`/`OBJ_STATUS` left at mainline's constants (unconfirmed cluster relevance per the DBC — see `PORT_LEAD_ICON_FROM_SUNNYPILOT.md` §3). Only active for CANFD Hyundais with `openpilotLongitudinalControl=True` (confirmed sole caller: `create_acc_cancel`, the only other `SCC_CONTROL` builder in this file, fires only when that flag is `False`). |
| `opendbc_repo/opendbc/car/hyundai/hyundaicanfd.py` | `create_acc_control()` signature + `values` dict | C | Tier 2: new optional `lead=None` param (`(dRel, vRel, present)`), packs `ACC_ObjDist`/`ACC_ObjRelSpd` only when `hud_control.leadVisible` AND `lead[2]` (`radarState.leadOne.present`) **both** agree — card's own fresher `radarState` read and the planner's slightly older view can disagree for a frame; fail-safe direction is "no number", matching the mapd port's 60 km/h fallback rule. Clipped to the DBC's documented ranges (`ACC_ObjDist` 0–204.7 m, `ACC_ObjRelSpd` −16.4–34.7 m/s) before packing. `ACC_ObjRelSpd` is **omitted entirely** (not set to 0) when no lead is shown — mainline never packed it either and its receiver is unconfirmed by the DBC, so explicitly writing a physical 0 would be a real behaviour change versus the packer's prior default. |
| `opendbc_repo/opendbc/car/hyundai/carcontroller.py` | `create_canfd_msgs()`, the `create_acc_control()` call site | C | Tier 2: passes `lead=getattr(CS, '_grt_lead', None)` — `CS` already reaches this function unchanged; `getattr` with a `None` default so any `CS` this branch hasn't touched (replay, tests) degrades to Tier 1's behaviour rather than raising. |

## Fork-owned files (category A — no merge conflicts)

- `openpilot/grt/` — `__init__.py`, `registry.py`, `hooks.py`, `settings.py`, `tests/`
  - mapd / set-speed: `scc_map.py`, `set_speed.py`
  - longitudinal comfort: `e2e_floor.py` (hook 6), `accel_ramp.py` (hooks 7 + 9),
    `hold_speed.py` (hook 8), `throttle_hold.py` (hook 10, layers A/B/C), `far_lead.py` (hook 11),
    `lead_filter.py` (hook 11b shadow + hook 11c display filter)
- `openpilot/common/grt_params_keys.inc` — param keys, `#include`d by `params_keys.h`
- `third_party/mapd/` — vendored fork binary + provenance `README.md`
- `PORT_MAPD_FROM_SUNNYPILOT.md`, `PORT_LEAD_ICON_FROM_SUNNYPILOT.md`, `FAR_LEAD_PREBRAKE_PROMPT.md`, `PROGRESS.md`, `GRT_MODS.md`, `captains_log.md`

## Sync procedure

1. `git rebase --onto <new-upstream-tag> <old-base> nightly-dev`.
2. Categories A/B should rebase clean. Resolve C/D by **meaning**.
3. `grep -rn "GRT-MOD"` and walk this table.
4. Re-run the wire-compat check (union discriminants pristine vs branch vs the Go schema) if
   anything in `cereal/` moved — see PROGRESS.md for the exact method.
5. Rebuild on the target device; never SCP generated schema artefacts.
